Prerequisites:
- Node package manager
- Node

Steps:
1. Run 'npm install' in the project folder via command line
2. Run the program with 'npm run start'
3. The browser should automatically open to localhost:3000